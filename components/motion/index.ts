// Shared motion primitives (built on motion/react, all reduced-motion-safe). Keep these the
// only "use client" leaves so pages/sections can stay Server Components.
export { Reveal } from "@/components/landing/reveal";
export { Stagger, StaggerItem } from "./stagger";
export { Parallax } from "./parallax";
